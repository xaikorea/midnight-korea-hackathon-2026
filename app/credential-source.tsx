import {type CredentialSource as Source} from '@/lib/domain';
export default function CredentialSource({source}:{source?:Source}){return <section className="compact-request"><h3>발급 근거</h3>{!source?<p>이 자격에는 서명에 연결된 문서 검토 기록이 없습니다. 입력한 속성의 서명 무결성만 확인합니다.</p>:<><span className="status warn">{source.kind==='synthetic'?'합성 시연 데이터':'담당자 문서 검토'}</span><dl className="detail-list"><dt>출처·참조번호</dt><dd>{source.reference}</dd><dt>대상 기간</dt><dd>{source.period}</dd><dt>확인 방법</dt><dd>{source.method}</dd><dt>확인자</dt><dd>{source.reviewer}</dd><dt>확인 시점</dt><dd>{new Date(source.reviewedAt).toLocaleString('ko-KR')}</dd></dl>{source.documents.map(d=><div key={d.id}><b>{d.name}</b><p className="journey-id">SHA-256 {d.sha256}</p></div>)}<p>{source.notice}</p><small>이 기록과 문서 해시는 자격 본문의 발급 서명에 포함됩니다.</small></>}</section>}
