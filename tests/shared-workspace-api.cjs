// Shared workspace regressions now enforce operational company ACLs, reviewed credentials,
// and actor-bound authority for both buyer and grant. Uses isolated fixtures.
require('./business-enforcement.cjs');
