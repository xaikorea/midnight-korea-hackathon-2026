const {chromium}=require(process.env.PLAYWRIGHT_PATH||'playwright');(async()=>{const b=await chromium.launch({channel:process.env.PLAYWRIGHT_CHANNEL||'chromium',headless:true});const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://localhost:5173/signin-with-chatgpt?return_to=/?view=companies',{waitUntil:'networkidle'});await p.goto('http://localhost:5173/?view=companies',{waitUntil:'networkidle'});console.log(await p.evaluate(()=>[...document.querySelectorAll('body *')].filter(x=>x.getBoundingClientRect().right>innerWidth+1).map(x=>[x.tagName,x.className,x.getBoundingClientRect().width]).slice(0,25)));await p.screenshot({path:'outputs/sap-supplier-mobile.png',fullPage:true});await b.close();})();
