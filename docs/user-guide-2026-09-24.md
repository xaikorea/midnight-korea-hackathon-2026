# 이용자 가이드 최신화 — 2026-09-24

공개 `/guide`와 업무 메뉴의 `/?view=guide`는 같은 `UserGuide`와 `lib/user-guide.ts` 내용을 사용한다.

- 기본 공개 체험은 준비된 자격으로 대상 선택 → 공유 확인·동의 → 한 번 제출의 세 단계로 안내한다. 역할 전환·등록·발급은 기본 신청의 선행 절차로 안내하지 않는다.
- 한빛테크의 합성 속성과 두 기관의 서로 다른 조건을 설명한다. 추가 자격 검사와 미충족·취소 비교는 선택형 상세 기능으로 분리한다.
- 실제 처리 관제의 4개 흐름·9개 단계, 서버 결과와 기록 재생의 차이, 재생 제어·동작 줄이기·기관 필터·JSON·이력 조회를 안내한다.
- 일부 실패·미충족·연결 끊김·세션 만료·새로고침 등 예외 상황에 대한 행동을 안내한다. 서버 저장, 조건 충족, 기관의 최종 결정과 실제 온체인 거래를 구분한다.
- 방문 분석의 이름 출처·쿠키 기준 통계·고객 전체 이력·월별 조회·서버 GeoIP·선택적 이름 등록·수집 중지를 현재 구현에 맞춘다.
- 별도 서비스 관리자 비밀번호 변경(8자 이상), 현재 기기의 로그인 유지와 다른 세션 만료를 설명한다. 인증 정보는 가이드에 포함하지 않는다.
- 기능별 안내 21개, FAQ 12개. 검색·목차·바로가기와 모바일 표시를 유지한다.

구현 기준: `docs/simplified-demo.md`, `docs/process-monitor.md`, `docs/visitor-analytics.md`, `docs/admin-password.md` 및 해당 화면·API 소스. 외부 문서의 사양으로 현재 서비스 동작을 추정하지 않는다.

검증 명령: `npm run typecheck`, 변경 파일 ESLint, `SMOKE_BASE`를 지정한 `tests/user-guide-ui.cjs`. 공개 검증은 가이드 조회와 별도 가상 체험 공간의 메뉴 이동만 수행하고 관리자 비밀번호 변경·실제 고객 정보 조회는 하지 않는다.

2026-09-24 NHN 공개 서비스에 이미지 `bizproof:guide-current-20260924` (`593cf2cbafed`)로 배포했다. 위 검사와 Linux 빌드 파이프라인, 격리/공개 브라우저 검증을 모두 통과했다. 공개 가이드와 로그인한 업무 메뉴 양쪽에서 21개 안내·12개 FAQ, 최신 날짜, 검색, 바로가기와 모바일 화면을 확인했다. 기존 데이터와 인증 설정은 유지했다.
