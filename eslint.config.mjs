import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
    "dist/**", ".wrangler/**", ".sites-runtime/**", ".tools/**", "integrations/**", "contracts/managed/**", "contracts/node_modules/**", "sidecar/node_modules/**", "outputs/**", "vendor/**", ".agents/**", ".codex/**",
  ]),
  {
    files: ["tests/**/*.cjs"],
    rules: {
      // These Node test runners are explicitly CommonJS, including their TS loader.
      "@typescript-eslint/no-require-imports": "off",
    },
  },
  {
    files: ["components/ui/**/*.{ts,tsx}", "hooks/use-mobile.ts"],
    rules: {
      // These files are vendored verbatim from shadcn@4.17.0. Keep the
      // registry source intact while applying the stricter rules to Site code.
      "@typescript-eslint/no-unused-vars": "off",
      "react-hooks/purity": "off",
      "react-hooks/set-state-in-effect": "off",
    },
  },
]);

export default eslintConfig;


