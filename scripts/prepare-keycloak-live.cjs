const fs=require('fs'),path=require('path'),{randomBytes}=require('crypto');
const root=path.resolve('.tools/keycloak/server/keycloak-26.7.4'),realm=JSON.parse(fs.readFileSync('integrations/keycloak/realm.json','utf8'));realm.realm='bizproof-live-v2';realm.sslRequired='none';const users=['admin','company','buyer','grant'].map(role=>({id:crypto.randomUUID(),username:'live-'+role,enabled:true,email:role+'@example.invalid',emailVerified:true,firstName:'Live',lastName:role,credentials:[{type:'password',value:randomBytes(24).toString('base64url'),temporary:false}],clientRoles:{'bizproof-web':[role]}}));const existing=path.join(root,'data/import/bizproof-live-v2.json');if(fs.existsSync(existing)){console.log('Existing isolated realm reused; no credentials regenerated.');process.exit(0);}realm.users=users;fs.mkdirSync(path.join(root,'data/import'),{recursive:true});fs.writeFileSync(path.join(root,'data/import/bizproof-live-v2.json'),JSON.stringify(realm));fs.writeFileSync('.tools/keycloak/live-users.json',JSON.stringify(users));console.log('Prepared isolated Keycloak test realm; credentials retained only in ignored local test files.');
